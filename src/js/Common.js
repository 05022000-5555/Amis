const Common = {
    format: function formatDateToString(date){
        if(data == null){
            return null
        }
        let tempDate = new Date(date)
        let day = tempDate.getDate();
        

    }
}